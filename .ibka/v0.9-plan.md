# v0.9 · 页面式笔记与整理（已批准）

用户批准将页面式编辑与 v0.9 一起实现，不重复请求实施批准。保留未提交的 v0.8 全部成果，不推送或部署。

设计：Redesign / Preserve；变化 3、动效 2、密度 5、资产依赖 1、品牌保真 10。宽屏主导航／紧凑列表／正文；空间不足时正文独占内容区。取消笔记编辑的遮罩、悬浮容器和关闭式导航，保留待办与课程现有短表单。正文纸面、衬线标题及现有深色令牌不变；低频工具与属性按需展开。

模块：M19 数据与整理（单层笔记本 CRUD、内置模板、笔记链接及来源待办）；M20 页面式写作与响应式列表；M21 本地图片及简单表格；M22 回归、文档与本地预览。web-design-engineer 负责实现，webapp-testing 负责实际交互回归。

图片边界：PNG/JPEG/WebP，本机解码及缩放后存储，不上传；单张原文件 10MB／16MP 以下，存储后 512KB 以下、最长边 1600px；保留 alt。独立资产表避免历史重复复制图片，完整 v6 备份包含笔记本和图片，读取 v1–v5。历史／草稿可能引用的旧资产在导入时保留，不以清理为名删除用户数据。

表格边界：无合并单元格、无公式，最多 20 行／8 列，支持增加／删除行列及删除表格，粘贴结构校验；使用现有 @tiptap/pm/tables，无新增生产依赖。

迁移：数据库仅添加 notebooks/assets 表，旧条目新增可选 notebookId/sourceNoteId，不批量解释旧文本。删除笔记本以事务移到未分类并递增版本；导入恢复含分类，图片 ID 内容冲突拒绝，旧草稿／历史仍可恢复。回退保留最新独立备份，不直接降级数据库。

验收：单元覆盖分类事务、链接/模板、图片校验、表格及 v6 往返；浏览器覆盖页面路由、后退、切换前保存、冲突、键盘、小屏深色、模板、链接、选段任务、离线图片和表格；旧任务/课程行为回归。真实手机输入法不以模拟测试代替。
