# 参与今日记开发

感谢帮助今日记变得更可靠。开始前请阅读 [AGENTS.md](AGENTS.md)、[架构说明](docs/architecture.md) 和 [开发流程](docs/development-workflow.md)。

## 提交问题

请说明设备、浏览器版本、复现步骤、实际结果和预期结果。截图、导出文件和控制台日志必须先移除私人笔记、课程、图片和其他个人信息。不要在公开 Issue 中上传完整备份。

## 开发原则

- 保持本地优先、无账户、无遥测和无远程正文服务。
- 保留旧数据、备份、草稿、历史和版本冲突保护。
- 复用现有 TypeScript、Dexie、Tiptap 和原生 CSS 能力；新增生产依赖前先讨论。
- 使用语义色和既有动效 token，覆盖手机、平板、桌面、深色、减少动态和键盘操作。
- 一个 Pull Request 聚焦一个结果，并包含相关测试和文档。

## 最低验证

```bash
npm ci
npm run check
npm test
npm run build
```

界面、导航、持久化或 PWA 修改还应运行 `npm run test:browser`。完整环境配置和套件说明见[开发与验证流程](docs/development-workflow.md)。

## Pull Request 内容

- 用户可见的变化和设计理由；
- 数据库、备份、旧数据和离线兼容性影响；
- 实际执行过的命令与结果；
- 界面修改的截图或可访问性证据；
- 尚未验证的真实设备或浏览器边界。

仓库目前没有为项目整体指定开源许可证。公开可见不代表已授予任意再分发权；第三方资源遵循各自许可证。
