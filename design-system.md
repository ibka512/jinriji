# 今日记设计系统

## Design Read

- 产品：便签、待办、日程与课程表融合的个人生活工具
- 受众：希望快速记录，同时需要安静、有秩序地安排日常的学生与个人用户
- 视觉语言：现代和风 × 液态玻璃 × 莫奈动态配色
- 模式：Redesign · Preserve
- 视觉变化度：3/10
- 动效强度：3/10
- 信息密度：5/10

## 定位

- 叙事角色：以“今日”为主线，记录与计划作为两翼
- 观看距离：手机约 30cm，桌面约 60cm
- 视觉温度：温暖、克制、安静
- 容量：手机单栏、平板双栏、桌面三栏；正文阅读宽度受限

## 设计决策

- 色彩：纸白与墨色为底，灰绿为主色，柿色为小面积强调；色值均通过语义 token 使用
- 字体：日期、页面标题、卡片标题与引文优先使用站点内置的 Noto Serif SC，并以新细明体等系统明朝体回退；正文与操作控件使用平台无衬线字体
- 间距：4px 基准，主要采用 8、12、16、24、32
- 圆角：内容纸张 22px、操作玻璃 28px；胶囊只用于移动端 Tab Bar、分段控件和状态标签
- 阴影：纸张低而柔，悬浮玻璃更深；暗色模式取消容器白色内高光，仅保留低对比边缘
- 动效：140–180ms 即时反馈，240–320ms 可打断面板变化；减少动态时切换为短促淡入淡出
- 无障碍：44px 点击目标、可见焦点、文字对比度、非颜色状态提示、降低透明度与减少动态降级
- 移动导航：四个顶级入口收纳进完整胶囊状液态玻璃 Tab Bar，由单个可移动高光透镜表达选中状态；快速记录使用等宽等高的独立圆形按钮
- 操作层级：每个视口只保留一个最醒目的新建入口；无行为的装饰控件不使用按钮语义
- 主题：同时支持系统深色模式、200% 字体缩放、320px 小屏、手机横屏与降低透明度模式
- 背景：使用单一灰绿和纸底色与极轻纸纤维纹理，不使用彩色页面渐变或暖色光斑
- 可读性：强调文字与实心按钮分开配对，实心按钮使用 `--action-bg` / `--on-action`；输入框和课程块使用各自的前景／背景令牌，不对文字施加整体亮度滤镜
- 设置：保留主题、数据备份与最近删除分组，以统一线性图标作为每项的前导；默认开启液态玻璃，不暴露材质开关，保留系统降低透明度支持
- 文案：省略重复小标题、口号与泛化提示；保留用户原文、时间地点和数据覆盖提示

## v0.5 日常操作

- 真实首页：接下来、今日待办、最近记录；日期与数量从真实记录派生，空状态只用一行说明和一个行动
- 记录：卡片预览可裁切，详情保留完整标题、正文和换行；搜索与类型筛选保持可见
- 详情：宽屏列表／正文并排，窄屏进入完整阅读页；返回恢复列表位置与筛选，不把主导航塞进弹窗
- 编辑：使用原生 dialog 的焦点／模态语义，手机按可视区域全屏、滚动内容与固定保存区分离；普通标题仍用系统无衬线输入，页面标题沿用明朝体
- 恢复：草稿自动暂存但不冒充正式保存；明确“舍弃草稿”；删除可撤销且可从设置恢复；导入与恢复必须预览确认
- 反馈：错误出现在字段附近并保留输入；待办状态不依赖颜色；重复保存禁用；页面动效不阻塞操作

## v0.6 课程安排

- 延续现有设计语言，课程表是数据界面，不增加装饰性统计与示例课程
- 计划分段采用 IBKA UI-001（`components/segmented-switch/demo.html`）：三个透明、等宽选项共用一个实体滑块；外层与滑块均为胶囊
- UI-001 只适配组件本身：主题取 `--control-bg` / `--selected-bg` / `--accent-deep`，不复制演示页的暖粉底色、渐变和长文案；选中位置只动画 `transform`，不动画宽度
- 切换使用现有 240ms `--ease-in-out`；按压为 140ms / .96，连续输入可重新定向；方向键、Home／End 及减少动态不产生滑动
- 学期控制保持单行优先，已有学期时“新建学期”使用带可访问名称的圆形加号；不额外占据手机整行
- 手机按日查看，极窄屏或大字号下日期条可在自身内部横向滚动；桌面用周时间轴，课程密集或平板宽度不足时仅课表局部滚动
- 宽屏课表按本周实际起止时间加一小时上下文收拢，重叠课程分列；无课周明确显示空状态
- 学期与调课表单使用原生模态 dialog、实色纸张背景及固定保存区；两列字段按实际字号和可用宽度变为一列，确保 200% 字号下日期可读
- 课程／便签通过已有详情页关联；停课明确标注并划线，不能仅靠颜色区分

## v0.7 导航与整理

- Preserve / Extension，变化 3、动效 3、密度 5、图像依赖 1、品牌保真 10；不改变主入口、路由、字体和语义色。
- 侧栏品牌使用纯文字「今日记」与原有小字，不再放置「今」字方块。宽屏侧栏为 208px，品牌、导航图标和底部状态对齐，紧凑侧栏保留可缩放的文字品牌。
- 两端导航共用原线框轮廓和实心镂空形态；状态使用 `aria-current`、字重、形态及背景共同表达，不只依赖颜色。切换形态即时生效，保留已有导航透镜和按压动效，不增加弹跳。
- 置顶在记录卡角落提供 44px 点击区域；标签为轻量文本，不嵌套装饰性卡片。筛选保留一个标签选择和置顶开关，批量工具仅在“整理”模式出现。
- 切换筛选或主页面清空批量选择，删除需确认；错误保留选择，整笔操作失败不留下半完成状态。
- 编辑器中的标签默认折叠，有已有内容时展开；重复选择只在待办编辑出现，日期与重复规则分工明确。
- 离线提示仅在断网时出现；设置说明缓存状态和本地数据边界，不使用“已同步”等虚假状态。
